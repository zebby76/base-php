---
name: Docker Build images

run-name: Docker Build ${{ github.ref_name }} by @${{ github.actor }}

concurrency:
  cancel-in-progress: true
  group: ${{ github.workflow }}-${{ github.ref }}

on:
  pull_request:
    branches:
      - main
      # Version branches (8.4, 8.5, ...). Without them a backport pull request
      # runs the linter only, so the change reaches its release tag having never
      # been built -- which is how the php-fpm memory cap shipped on 8.4.
      - "[0-9]+.[0-9]+"
    paths-ignore:
      - "docs/**"

  push:
    branches:
      - main
    tags:
      - "[0-9]+.[0-9]+.[0-9]+*"

  workflow_dispatch:
    inputs:
      version:
        description: "Docker Image Version"
        required: false
        type: string

env:
  DOCKER_IMAGE_NAME: ${{ github.repository == 'Smals-Webtech/base-php' && ((github.event_name == 'workflow_dispatch' && inputs.version) || startsWith(github.ref, 'refs/tags/') || (github.ref == 'refs/heads/main' && github.event_name != 'pull_request')) && 'smalswebtech/base-php' || 'smalswebtech/base-php-dev' }}

jobs:
  prepare:
    runs-on: ubuntu-24.04

    outputs:
      # Push if it's a tag, a workflow_dispatch with version, or a commit to the main branch --
      # and only from the canonical repository. A fork that has Actions enabled would otherwise
      # publish under the upstream production image name.
      push: ${{ github.repository == 'Smals-Webtech/base-php' && ((github.event_name == 'workflow_dispatch' && inputs.version) || startsWith(github.ref, 'refs/tags/') || (github.ref == 'refs/heads/main' && github.event_name != 'pull_request')) && true || false }}
      sbom: ${{ github.event_name != 'pull_request' }}
      provenance: ${{ github.event_name != 'pull_request' }}
      ref: ${{ (github.event_name == 'workflow_dispatch' && inputs.version) || '' }}
      platforms: ${{ steps.matrix.outputs.platforms }}
      targets: ${{ steps.matrix.outputs.targets }}
      variants: ${{ steps.matrix.outputs.variants }}
      metadata: ${{ steps.matrix.outputs.metadata }}

    steps:
      - name: Checkout source
        uses: actions/checkout@v6

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4

      - name: Create platforms, targets and variants matrix
        id: matrix
        shell: bash
        run: |
          set -e
          # shellcheck disable=SC2046,SC2086
          METADATA="$(docker buildx bake --print | jq -c)"
          TARGET_KEYS=$(jq -r '.target | keys[]' <<<"${METADATA}" | grep -E '.+-.')
          VARIANTS=$(echo "${TARGET_KEYS}" | cut -d'-' -f1 | sort -u)
          TARGETS=$(echo "${TARGET_KEYS}" | cut -d'-' -f2 | sort -u)
          {
            echo metadata="${METADATA}"
            echo platforms="$(jq -c 'first(.target[]) | .platforms' <<< "${METADATA}")"
            echo targets="$(jq -nc --argjson arr "$(printf '%s\n' "${TARGETS}" | jq -R . | jq -s .)" '$arr')"
            echo variants="$(jq -nc --argjson arr "$(printf '%s\n' "${VARIANTS}" | jq -R . | jq -s .)" '$arr')"
          } >> "${GITHUB_OUTPUT}"
        env:
          GIT_HASH: ${{ github.sha }}
          DOCKER_IMAGE_VERSION: ${{ (github.ref_type == 'tag' && github.ref_name) || (github.event_name == 'workflow_dispatch' && inputs.version) || 'snapshot' }}

  build:
    runs-on: ${{ startsWith(matrix.platform, 'linux/arm') && 'ubuntu-24.04-arm' || 'ubuntu-24.04' }}

    needs:
      - prepare

    strategy:
      fail-fast: false
      matrix:
        platform: ${{ fromJson(needs.prepare.outputs.platforms) }}
        target: ${{ fromJson(needs.prepare.outputs.targets) }}
        variant: ${{ fromJson(needs.prepare.outputs.variants) }}

    permissions:
      contents: read
      packages: write
      id-token: write
      attestations: write

    steps:
      - name: Prepare
        id: prepare
        run: |
          platform=${{ matrix.platform }}
          echo "sanitized_platform=${platform//\//-}" >> "${GITHUB_OUTPUT}"

      - name: Checkout source
        uses: actions/checkout@v6
        with:
          ref: ${{ needs.prepare.outputs.ref }}
          fetch-depth: 1

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4
        with:
          platforms: ${{ matrix.platform }}

      - name: Login to DockerHub
        uses: docker/login-action@v4
        # Secrets are not exposed to pull requests from forks, so the login can
        # only fail there. Such a build never pushes anyway (push is false), so
        # skip it and let the build run with anonymous pulls. Same guard the
        # push job already carries.
        if: github.event_name != 'pull_request' || github.event.pull_request.head.repo.full_name == github.repository
        with:
          registry: docker.io
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_PASSWORD }}

      - name: Bake and push image
        id: build
        uses: docker/bake-action@v7
        with:
          source: .
          pull: true
          load: ${{ !fromJson(needs.prepare.outputs.push) }}
          sbom: ${{ fromJson(needs.prepare.outputs.sbom) }}
          provenance: ${{ fromJson(needs.prepare.outputs.provenance) }}
          targets: |
            ${{ matrix.variant }}-${{ matrix.target }}
          files: |
            docker-bake.hcl
          # Tags are removed on the push path to prevent a "can't push tagged ref
          # [...] by digest" error. On the load path they are what gives the test
          # step an image to run, so they are kept.
          set: |
            ${{ fromJson(needs.prepare.outputs.push) && '*.tags=' || '' }}
            *.platform=${{ matrix.platform }}
            ${{ matrix.variant }}-${{ matrix.target }}.cache-from=type=gha,scope=${{ matrix.variant }}-${{ matrix.target }}-${{ needs.prepare.outputs.ref || github.ref }}-${{ matrix.platform }}
            ${{ matrix.variant }}-${{ matrix.target }}.cache-from=type=gha,scope=refs/heads/main-${{ matrix.variant }}-${{ matrix.target }}-${{ matrix.platform }}
            ${{ matrix.variant }}-${{ matrix.target }}.cache-to=type=gha,scope=${{ matrix.variant }}-${{ matrix.target }}-${{ needs.prepare.outputs.ref || github.ref }}-${{ matrix.platform }},ignore-error=true
            ${{ fromJson(needs.prepare.outputs.push) && format('*.output=type=image,name={0},push-by-digest=true,name-canonical=true,push=true', env.DOCKER_IMAGE_NAME) || '' }}
        env:
          GIT_HASH: ${{ github.sha }}
          DOCKER_IMAGE_VERSION: ${{ (github.ref_type == 'tag' && github.ref_name) || needs.prepare.outputs.ref || 'snapshot' }}

      # Only the load path has an image to run, and one architecture is enough to
      # exercise the configuration the suites assert on. A published build is
      # made of the same commits, each of which came through a pull request that
      # ran this step.
      - name: Set up bats
        if: matrix.platform == 'linux/amd64' && !fromJson(needs.prepare.outputs.push)
        uses: bats-core/bats-action@4.0.0
        with:
          # Pinned so a local run and a CI run exercise the same bats. The
          # assertion helpers live in test/lib, vendored, so the libraries this
          # action can also install are not wanted here.
          bats-version: 1.10.0
          support-install: false
          assert-install: false
          detik-install: false
          file-install: false

      - name: Test image
        if: matrix.platform == 'linux/amd64' && !fromJson(needs.prepare.outputs.push)
        working-directory: test
        env:
          DOCKER_IMAGE_VERSION: snapshot
          BATS_VARIANT: ${{ matrix.variant }}
          BATS_TARGET: ${{ matrix.target }}
        run: |
          case "${BATS_VARIANT}" in
            cli)          suite=tests.cli.bats ;;
            nginx|apache) suite=tests.web.bats ;;
            *)            echo "no image suite for the ${BATS_VARIANT} variant yet"; exit 0 ;;
          esac
          bats --print-output-on-failure "${suite}"

      - # Workaround for https://github.com/actions/runner/pull/2477#issuecomment-1501003600
        name: Export metadata
        if: fromJson(needs.prepare.outputs.push)
        run: |
          mkdir -p "/tmp/metadata/${{ matrix.variant }}-${{ matrix.target }}"
          digest=$(jq -r '."${{ matrix.variant }}-${{ matrix.target }}"."containerimage.digest"' <<< "${METADATA}")
          touch "/tmp/metadata/${{ matrix.variant }}-${{ matrix.target }}/${digest#sha256:}"
        env:
          METADATA: ${{ steps.build.outputs.metadata }}

      - name: Upload ${{ matrix.variant }}-${{ matrix.target }} metadata
        if: fromJson(needs.prepare.outputs.push)
        uses: actions/upload-artifact@v7
        with:
          name: metadata-${{ matrix.variant }}-${{ matrix.target }}-${{ steps.prepare.outputs.sanitized_platform }}-${{ github.sha }}
          path: /tmp/metadata/${{ matrix.variant }}-${{ matrix.target }}/*
          if-no-files-found: error
          retention-days: 1

  # Adapted from https://docs.docker.com/build/ci/github-actions/multi-platform/
  push:
    runs-on: ubuntu-24.04

    permissions:
      id-token: write
      packages: write
      contents: read
      attestations: write

    needs:
      - prepare
      - build

    if: fromJson(needs.prepare.outputs.push)

    strategy:
      fail-fast: false
      matrix:
        target: ${{ fromJson(needs.prepare.outputs.targets) }}
        variant: ${{ fromJson(needs.prepare.outputs.variants) }}

    steps:
      - name: Download metadata
        uses: actions/download-artifact@v8
        with:
          pattern: metadata-${{ matrix.variant }}-${{ matrix.target }}-*
          path: /tmp/metadata
          merge-multiple: true

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4

      - name: Login to DockerHub
        uses: docker/login-action@v4
        if: github.event_name != 'pull_request' || github.event.pull_request.head.repo.full_name == github.repository
        with:
          registry: docker.io
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_PASSWORD }}

      - name: Create manifest list and push
        working-directory: /tmp/metadata
        run: |
          set -x
          # shellcheck disable=SC2046,SC2086
          docker buildx imagetools create $(jq -cr '.target."${{ matrix.variant }}-${{ matrix.target }}".tags | map("-t " + .) | join(" ")' <<< ${METADATA}) \
            $(printf "${DOCKER_IMAGE_NAME}@sha256:%s " *)
        env:
          METADATA: ${{ needs.prepare.outputs.metadata }}

      - name: Inspect image
        run: |
          set -x
          # shellcheck disable=SC2046,SC2086
          docker buildx imagetools inspect $(jq -cr '.target."${{ matrix.variant }}-${{ matrix.target }}".tags | first' <<< ${METADATA})
        env:
          METADATA: ${{ needs.prepare.outputs.metadata }}
