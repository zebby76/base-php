# Read by /usr/local/bin/aws. Data only -- no execute permission required.
AWS_CONFIG_FILE="{{ .Env.AWS_CONFIG_FILE }}"
AWS_SHARED_CREDENTIALS_FILE="{{ .Env.AWS_SHARED_CREDENTIALS_FILE }}"
AWS_PROFILE="{{ .Env.AWS_PROFILE }}"
